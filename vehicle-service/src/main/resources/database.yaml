spring:
  datasource:
    url: jdbc:mariadb://mariadb:3306/ebikedb
    password: EBikePassword
    username: EBikeApplication
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
